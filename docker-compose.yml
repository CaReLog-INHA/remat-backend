services:
  app:
    image: ${IMAGE:-ghcr.io/zeoueon/remat-backend:latest}
    container_name: remat-app
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
